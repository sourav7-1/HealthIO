import { PortalPlaceholder } from "@/components/portal-placeholder";

export const metadata = { title: "Admin portal" };

export default function Page() {
  return <PortalPlaceholder title="Admin portal" phase={21} />;
}
