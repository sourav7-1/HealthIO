import { PortalPlaceholder } from "@/components/portal-placeholder";

export const metadata = { title: "Caregiver portal" };

export default function Page() {
  return <PortalPlaceholder title="Caregiver portal" phase={6} />;
}
