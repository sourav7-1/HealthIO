import { PortalPlaceholder } from "@/components/portal-placeholder";

export const metadata = { title: "Doctor portal" };

export default function Page() {
  return <PortalPlaceholder title="Doctor portal" phase={4} />;
}
