import { PortalPlaceholder } from "@/components/portal-placeholder";

export const metadata = { title: "Patient portal" };

export default function Page() {
  return <PortalPlaceholder title="Patient portal" phase={5} />;
}
